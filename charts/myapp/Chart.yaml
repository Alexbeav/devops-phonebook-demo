apiVersion: v2
name: myapp
version: 0.1.0
description: Helm chart for the phonebook demo (Node.js/Express + React + PostgreSQL)
dependencies:
  - name: postgresql
    version: 18.11.3
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
